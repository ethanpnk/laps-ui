### Added
- LAPS (Intune) tab with Microsoft Graph sign-in and device search.
- Automatic installation of the Microsoft.Graph PowerShell module when missing.
- Entra LAPS retrieval via `directory/deviceLocalCredentials` with fallbacks to Intune action.

### Improved
- Safer Microsoft Graph URI building and Entra device object ID resolution.
- Better error handling and messaging around Graph sign-in and queries.
